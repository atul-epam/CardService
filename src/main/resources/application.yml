server:
  port: 8080

spring:
  application:
    name: cardservice

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/carddb
    password: admin
  jpa:
    hibernate:
      ddl-auto: update

